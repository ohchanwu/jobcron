// Package scraper holds the shared job-posting domain model (Posting, Tag)
// and — once sources are implemented — the Scraper interface and source
// registry. Concrete sources (e.g. 점핏) live in subpackages.
package scraper
