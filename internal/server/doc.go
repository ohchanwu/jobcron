// Package server runs the HTTP server: route registration, the embed.FS for
// web/ assets, the SSE scrape stream, the per-source single-flight scrape
// mutex, and cross-platform browser launch.
package server
